apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ .Values.ibm-ace-operator.subscriptions.ibmace.name }}
  annotations:
spec:
  {{- toYaml .Values.ibm-ace-operator.subscriptions.ibmace.subscription | nindent 2 }}
